from examples.feature.components.ui import Button, Card

def Page(title):
    return (
        <Card>
            <h1>{title}</h1>
            <Button variant="primary">
                Absolute import works
            </Button>
        </Card>
    )
